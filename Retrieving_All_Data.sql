--Retrieving all columns of data and also all records of table(employee_payroll)
SELECT * 
FROM employee_payroll;

--retrieving all records of particular columns that we want from table(employee_payroll)
SELECT SALARY , NAME  
FROM employee_payroll;

